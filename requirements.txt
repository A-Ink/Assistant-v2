# ==========================================
# ALLIANCE TERMINAL — DEPENDENCY MANIFEST
# ==========================================

# --- Core UI & Subsystems ---
pywebview>=5.0
psutil>=5.9.0
chromadb>=0.5.0
huggingface-hub
pydantic>=2.0.0
PyYAML

# --- ENGINE 1: NPU (OpenVINO Core) ---
# STRICT LOCK: These exact versions prevent the "Architecture Gap" 
# and CLI compilation crashes when downloading new models.
transformers==4.57.0
optimum-intel[openvino,nncf]==1.27.0
openvino==2026.0.0
openvino-genai==2026.0.0
openvino-tokenizers==2026.0.0

# --- ENGINE 2: iGPU (Vulkan Core) ---
# NOTE: llama-cpp-python requires C++ Compilation on Windows to map to Vulkan properly.
# It has been explicitly removed from this file to prevent CI auto-build crashes.
# Please run setup.ps1 to interactively compile and install it natively.